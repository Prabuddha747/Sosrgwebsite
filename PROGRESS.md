# SosrG Redesign — Progress Log

**Purpose:** audit trail of the redesign effort per `doc/implementation.md`'s phase plan, so a session can resume without re-deriving context. Read this, then `doc/architecture.md`, `doc/flow.md`, `doc/redesign.md`, `doc/implementation.md`, `doc/schema.md` in that order before writing any code, per the standing instructions.

**Last updated:** 2026-08-09

---

## Repo / branch state

- Remote: `https://github.com/digitalsosrg/SosrgWebApp`
- `main` has Phase 0, Phase 1, and this session's Phase 2 (global shell + live API integration) work — **committed directly to `main`, not a phase branch.** `implementation.md` §0 rule 2 calls for a branch per phase; this session didn't follow that. Flagging rather than silently normalizing it — next session should decide whether to retroactively branch/tag or accept `main`-direct as the pattern going forward.
- Tags on `main`: `baseline-pre-redesign` (pre-redesign snapshot), `phase-0-complete`, `phase-1-complete`. No `phase-2-complete` tag yet — pending the open items listed under Phase 2 below.
- Branch `phase-2-global-shell` still exists, still has **no commits** — this session's Phase 2 work landed on `main` instead, so this branch is now stale relative to `main`, not "safe to continue on" as previously noted.
- CI (`.github/workflows/ci.yml`) is currently **paused** — `workflow_dispatch` only, not triggered automatically on push/PR. This was intentional (rapid iteration across phase branches) but **must be restored to push/pull_request triggers before the overall redesign effort is considered done** — don't forget this. Still not actioned this session either.

---

## Phase 0 — Foundation & safety net: DONE, merged, tagged

- Removed unused deps (`express`, `better-sqlite3`, `@types/express`) — confirmed via grep no real code referenced them.
- Added `react-router-dom` as a dependency (not wired anywhere yet — that's Phase 2's job).
- Split the single 10,434-line `src/App.tsx` into `types/`, `data/`, `components/{layout,home,marketplace,profile}/`, `pages/` — pure mechanical extraction (scripted line-range slice, not manual retyping, to guarantee zero transcription drift), zero visual/behavior change. Verified via `tsc --noEmit`, `vite build`, and a live click-through (Home, Casting, Admin, Creator Profile/Wallet) with zero console errors.
- Old pages still use the **old** design system (`src/index.css`'s `gold`/`crimson`/`cinematic-black` tokens, old `Navbar`/`Footer` in `components/layout/`) — nothing about their visuals has changed.

**Known open item carried from this phase:** `implementation.md` line 3 says the backend is "MySQL 8.4 + GCS" but `schema.md` itself targets "PostgreSQL 15+" and uses Postgres-only features (native `ENUM`, `JSONB`, `gen_random_uuid()`, `CITEXT`, PostGIS `GEOGRAPHY`) throughout. **Not resolved — flag to whoever owns the backend before Phase 11 (API connection readiness).**

**Also flagged, not yet actioned:** a live Gemini API key was displayed in this session's transcript when inspecting `.env.local` (it was never committed — `.gitignore` correctly excludes `.env*`). Worth rotating since it's now sitting in a chat transcript, purely as hygiene.

---

## Phase 1 — Design tokens & component library: DONE, merged, tagged

**Everything here is net-new and additive — no live page was touched.** All new tokens/classes are deliberately namespaced (`navy-*`, `gold-500/300/700`, `text-sosrg-*`, `tablet:/laptop:/desktop:` breakpoints, `shadow-elevation-*`) so they can't collide with or override the legacy tokens old pages still use.

- `src/design-system/design-tokens.json` — canonical token values (colors, elevation shadow recipes, fluid type scale, breakpoints, container, focus ring, the new heading-settle motion primitive). Hand-kept in sync with:
- `src/design-system/tokens.css` — the actual Tailwind v4 `@theme` block + supporting utility classes (`.sosrg-focus-ring`, `.sosrg-container`, `.sosrg-heading-settle`), imported into `src/index.css` alongside (not replacing) the old `@theme` block.
- **New tokens added beyond redesign.md's original palette**, each with a stated reason and measured contrast (same relative-luminance method redesign.md §1 used, hand-verified against its own stated ratios first): `--color-tier-yellow` (#F59E0B, 6.67:1 vs navy-900) and `--color-tier-blue` (#60A5FA, 5.64:1 vs navy-900) — needed for the 4-tier account Badge, since redesign.md's palette only had semantic colors for 2 of the 4 tiers (Green→success, Red→danger already existed).
- Full component library in `src/design-system/components/`: `Button`, `Card`, `Badge` (+ `tierColorVar` export), `Modal`, `Toast` (+`ToastProvider`/`useToast`), `Tabs`, `Input`/`Select`/`Textarea`, `Avatar`, `Skeleton`, `EmptyState`, `Navbar` (isolated — see below), `SettledHeading`. Barrel export at `src/design-system/index.ts`.
- **Navbar built in Phase 1 is intentionally NOT wired into the live app** — it's the isolated, showcase-verified version only. It takes a generic `groups`/`bottomTabItems` prop shape and renders plain `<a href>` tags. Real `<Link>` wiring + real route data + role-gated Admin visibility is explicit Phase 2 work (see below).
- **Footer was NOT built in Phase 1** — `redesign.md` §6's component table doesn't list it (only `implementation.md`'s Phase 2 checklist does). Still needs building.
- Dev-only component showcase at `dev-components.html` → `src/dev/`. Confirmed excluded from the production build (`npm run build`'s output JS has zero references to any design-system/dev code; not in `vite.config.ts`'s build input). Live-tested every component + interaction (Modal focus-trap/Escape/backdrop-close, Toast, Tabs keyboard nav, mega-menu) with zero console errors. Breakpoint media queries confirmed compiled correctly in a real build (`desktop:` → `min-width:1025px`, `max-tablet:` → `max-width:481px`).
- **React Bits jsrepo registry is still unresolved** — `reactbits.dev`'s documented init URLs (`/tailwind/`, `/default/`, `/r/tailwind`) all return the SPA's HTML shell instead of a JSON manifest when probed. Not blocking so far — `motion/react` (already a dependency) was used as a drop-in stand-in everywhere redesign.md's §5 table names a React Bits component (Modal/Toast enter-exit). This will need solving for real before Phase 3 needs `Aurora`/`SplitText`/`TiltedCard`/`CountUp`/`Marquee`.
- **Mid-Phase-1 design decision, made live with the user and written into `redesign.md` itself (§3 and §5):** the platform's one repeatable typographic motion idea is Fraunces' own variable `wght`/`opsz` axes transitioning from a lighter/smaller resting state to full weight/optical-size (~600ms, CSS `font-variation-settings`, no JS animation library) — three triggers, one mechanism: load+shimmer stays reserved for the hero headline only (unchanged from the original spec), scroll-into-view-once for every other section heading, hover/focus-retriggerable for card titles acting as links. Implemented as the `SettledHeading` component. Reduced-motion renders directly at the full settled state.
- **Accessibility pass complete:** one real contrast failure found and fixed (Button `destructive` variant: `text-primary` on `bg-danger` measured 4.42:1, under AA's 4.5:1 — switched to `text-white`, 4.90:1, for that variant only). Everything else checked out (Badge's colored text turned out to already be covered by the existing danger/success-vs-navy measurements once it was clear Badge renders as a soft tint pill, not a solid fill — an earlier commit message over-flagged this).

---

## Decision log (things decided this session, not guessed)

1. **Spacing scale:** no custom spacing tokens defined — Tailwind v4's default scale already produces exactly the 4/8/12/16/24/32/48/64/96/128px scale `redesign.md` §4 specifies.
2. **Background images / solid background color (user-confirmed):** the "no background images, solid navy-950" treatment applies to the **Phase 1 showcase only** for now. Live pages keep their picsum.photos background images until Phase 3 (Homepage) actually rebuilds them — this was an explicit choice to preserve one-phase-at-a-time discipline rather than jump Phase 3 work forward.
3. **Heading motion effect (user-confirmed):** see the Fraunces variable-axis settle effect above — now written into `redesign.md` §3/§5 as the source of truth, not just implemented ad hoc.
4. **CI paused (not removed):** `.github/workflows/ci.yml` triggers switched to `workflow_dispatch` only during this fast-iteration stretch. **Must be restored before the redesign is considered fully done.**
5. **Phase 2 routing scope (user-confirmed, this is the big one for next session):** when Phase 2 replaces the current `activeSection`-switch navigation with real React Router routes —
   - **Only the locked v1 scope gets real, functional routes:** Home, Casting, Events, Community, Profiles (creator/business), CP/Ecosystem, Admin.
   - **Everything outside that scope (Marketplace, Auction, Academy, AI Suite, Talent Directory, SosrG 7E) gets a "Coming soon"-style placeholder route/page instead of its current real content** — explicitly rejected the alternative of leaving them on the old unrouted mechanism (that would mean running two navigation systems side by side).
   - The user wants these placeholder pages to **cross-promote the mobile app** — copy along the lines of "this feature is coming / available in our app, download it to access X now" — **exact copy, CTA target/link, and whether an app even exists/has a store listing yet were not specified — this is an open product question for next session, don't guess the app store link or invent marketing copy wholesale.**

**Items 5 (in part) is superseded below (2026-08-09 entry) — left here, not deleted, so the reasoning trail (what was decided when, and why it changed) stays intact.**

6. **(2026-08-09) Skip the mock-service-layer step entirely.** `implementation.md` §3's typed-interface + mock-adapter + real-adapter + `USE_MOCK` flag pattern is **not being built**. Every service (`auth`, `profiles`, `casting`, more to follow) has exactly one implementation, written directly against the live API. The typed-interface shape is kept (costs nothing, matches repo convention) but there's no mock to swap out. Confirmed directly with the user via a clarifying question rather than inferred from a peer session's handoff that proposed it.
7. **(2026-08-09) Coming Soon copy: generic placeholder, no app-store CTA.** Resolves item 5's open question. Out-of-scope pages get a plain "Coming soon" marker with no app-store link and no invented marketing copy — confirmed directly with the user rather than guessed.
8. **(2026-08-09) "Coming Soon" means bannered, not replaced — overrides item 5's "gets a placeholder route/page instead of its current real content."** Every page — both out-of-scope ones and locked-v1-scope ones not yet wired to the live API — keeps its full existing content, with a `ComingSoonBanner` marking what's real vs. preview. Nothing gets replaced by an empty state. This extends to any component swap, not just routing: an initial pass had replaced the old feature-rich `Navbar` (logo, language switcher, theme toggle, mega-menu) with a thinner new one, and the old 23-tab `ProfileSystem` account dashboard with a minimal new profile card — the user caught both directly ("you vanish a lot of sections... I don't want you to vanish all those things, any of it") and both were reverted to the old component, with real data injected into their headers instead of a wholesale replacement. This is now the standing rule for every future page.
9. **(2026-08-09) Design system pivot: cream/gold light theme, replacing the v2 navy dark theme.** The user shared real screenshots of the existing SosrG mobile app (cream/ivory background, gold accents, neumorphic cards, a "SOON" badge pattern already in production there) and asked the web redesign to fully match it, not just take layout cues — confirmed directly via a clarifying question before touching anything ("full visual match" was chosen over the lower-cost "layout only, keep navy dark theme" option). All colors were re-sourced by pixel-sampling the real screenshots (Pillow) and re-verified for AA contrast against the new cream backgrounds — same discipline `redesign.md` §1 used for the original navy palette, documented in `design-tokens.json`'s and `tokens.css`'s own header comments. `redesign.md` itself still describes the v2 navy theme and needs a changelog pass next session (flagged below).
10. **(2026-08-09) React Bits is still blocked, confirmed not a regression.** Re-tested this session: `reactbits.dev`'s jsrepo registry endpoints return HTTP 200 but with the SPA's HTML shell instead of a JSON manifest — `npx jsrepo list` fails identically to Phase 1's original finding. `motion/react` remains the sanctioned stand-in; did not adopt the unofficial `@appletosolutions/reactbits` npm package found on the registry, since it isn't affiliated with the real reactbits.dev project and its component API wasn't verified to match what `redesign.md` §5's table names.

---

## Phase 2 — Global shell + real API integration: DONE, diverged significantly from the original plan (2026-08-09)

Executed directly on `main`, not the `phase-2-global-shell` branch (that branch is still empty — this session's work should probably be reconciled with or replace it; flagged, not silently decided). Per the decision log above, this phase ended up bundling live API integration in, since the user explicitly chose to skip the mock-service-layer step (decision #6) rather than do Phase 2 (shell) and Phase 11 (API readiness) as separate later phases per `implementation.md`'s original ordering.

- [x] React Router wired for real — `BrowserRouter`/`AuthProvider`/`ToastProvider` mounted in `main.tsx`, `App.tsx` converted from `activeSection` state-switch to `<Routes>`. Old `activeSection`-driven children (Hero, TalentGrid, SmartAssistant, DynamicBackground) unchanged — bridged via a `setActiveSection(s: Section) → navigate(sectionPath[s])` shim in `src/lib/sectionRoutes.ts` rather than rewritten.
- [x] Real routes for locked scope: `/`, `/casting`, `/events`, `/community`, `/profile`, `/ecosystem`, `/admin` (role-gating deferred — see open item below) — plus new auth routes `/login`, `/signup`, `/profile/setup`, `/profile/:username` (public).
- [x] "Coming soon" for out-of-scope routes — **per decision #8, these keep their full existing content** (banner-marked, not replaced), diverging from the original plan's "placeholder route/page instead of current real content."
- [x] Old `components/layout/Navbar.tsx` kept and wired to the same `setActiveSection` shim (real routes now, same rich UI — logo, language switcher, theme toggle, mega-menu, Join Premium CTA) — **not** replaced with the Phase-1 design-system `Navbar`, per decision #8. That component still exists, still works (verified in the dev showcase with `MemoryRouter`), just isn't live yet.
- [x] Auth wired for real: register → profile-setup → own profile, login, logout, public profile view — all curl- and browser-verified end-to-end against the live API (see `doc/API_INTEGRATION_LOG.md`). No mock auth service was built (decision #6).
- [x] Casting Calls tab (inside the existing `CastingEcosystem` component) wired to the live API — list/get/apply all verified live in-browser, including a real submitted application. Every other tab on that page (Live Audition Studio, Forum, Workshops, Mentorship, etc.) is untouched, still decorative, still marked by the page-level banner.
- [x] Old `ProfileSystem` (23-tab account dashboard) restored under `/profile` below the new real-data card, with its header's name/type fields now sourced from the real profile (`realProfile` prop) and every other header field it can't back with real data (platform ID, rating, industry tags, Green ID badge) replaced with an honest "Missing info" marker instead of the original mock values. "Edit Profile" now shows an honest "under development" toast instead of opening the old decorative setup wizard.
- [x] `doc/schema.md` corrected: MySQL 8.4 (not Postgres), plus a new §0 reconciling it against the real backend's actual migrations — several sections it describes (CP franchise, the Yellow/Green/Blue/Red account-tier system, Talent Auctions, the wallet/coin model) have no backing table at all in the real schema. Full detail in that file's own §0, not duplicated here.
- [x] `doc/API_REQUIREMENTS.md` written — structural gaps in the live API (unpopulated `fieldErrors`, undocumented response schemas, no org display-name resolution, undocumented status enum) plus a priority-ordered list of modules with zero live coverage, cross-checked against what the DB schema already models.
- [ ] **Not done — Job Postings.** Same shape as Casting Calls, deferred to next session, not forgotten (tracked as its own item).
- [ ] **Not done — role-gated Admin.** `/admin` is behind `RequireAuth` (must be logged in) as a baseline safety default, but there is no real "is this user an admin" signal anywhere in the live API response (`AuthAccount.accountType` was only ever observed as `"public_user"`) — flagging this as a real open question for the backend team (also noted in `API_REQUIREMENTS.md` §3) rather than inventing a check.
- [ ] **Not done — `Footer`.** Still using the old one; never got to building the Phase-1 version.
- [ ] **Not done — theme provider / light-mode architecture.** The cream/gold pivot (decision #9) was done as a direct token-value rewrite in `design-tokens.json`/`tokens.css`, not as a component-level theme-provider abstraction with a dark/light switch — there's only one theme now (cream), and the old dark navy tokens are gone, not toggleable. If the product ever needs both, that's new work, not a flip of an existing switch.
- [ ] **Not done — `redesign.md` changelog.** The design doc still describes the v2 navy theme as current; needs a §1 rewrite/changelog entry reflecting decision #9. The actual new values are documented in `design-tokens.json`'s and `tokens.css`'s header comments in the meantime.
- [ ] **Not done — CI restoration.** Still flagged from Phase 0/1, still not actioned.

**First thing next session should do:** wire Job Postings (same pattern as Casting Calls, service layer already scaffolded as a template in `src/services/casting/`), then reconcile the `phase-2-global-shell` branch (still empty) with the fact that this work landed on `main` directly.
